Given resources/js/pages/portal/guest/RegistrationUserForm.vue our current student registration,
We need to reconsider and redesign everithing.Given that we have new students to the college, 
and returning students who are coming back to continue, also that we have student number 
and national id number as unique fields. How can we create an enrolment 
work flow that helps with to avoid duplicate user accounts. 
Create a registration / authentication workflow and design a frontend using vuejs and tailwind css,
 - The National ID is the single source of truth. Every enrollment path — new or returning — runs a lookup against it before proceeding:

New student tries to register with an existing National ID → they see a red "Existing Record Detected" warning with the name partially masked for privacy, and a prompt to switch to the returning student flow instead. Registration is blocked.
Returning student must enter their National ID (and optionally their student number) → the system finds and surfaces their existing record before asking for a password, so they can never accidentally create a second account.
Student Number is a secondary unique identifier used for returning student lookup if the National ID is forgotten.

What's in the flow:
Step
- New Student
    -1. National ID duplicate check (validate id number infront end using useUtils.isZimbabweanNationalId())
    -2. Personal details form, create user account. 
- Returning Student 
    -1. National ID / Student No. lookup (validate id number infront end using useUtils.isZimbabweanNationalId()),mif its id number else student we dont validate we just look up.
    -2. Redirect to login and redirect to level -> program selection (as is).

Redesign the page to a more mobile first response
- Show these Alerts 
    1. 'Attention EcoCash users: To avoid network failures, please use separate devices when making your payments. Thank you for your cooperation.'
    2. 'Check the official College Advert before  making an online application'
- Wire the design beautifully using exisiting components where possible.
- Improve the backend using  the recently installed larvel json api where applicable