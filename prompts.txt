
<?php

namespace App\Models\Students;

/**
 * @mixin Builder
 *
 * @method static filter(Filter $filters)
 */
class StudentEnrolment extends Model
{
    use Filterable, LogsActivity,Paginatable, SoftDeletes;

    protected $fillable = [
        'student_id',
        'student_program_id',
        'institution_department_id',
        'department_level_id',
        'department_course_id',
        'academic_year_option_id',
        'academic_calendar_id',
        'mode_of_study_id',
        'student_enrolment_status_id',
    ];

    public function student(): BelongsTo
    {
        return $this->belongsTo(Student::class, 'student_id');
    }

    public function studentProgram(): BelongsTo
    {
        return $this->belongsTo(StudentProgram::class, 'student_program_id');
    }

    public function institutionDepartment(): BelongsTo 
    {
        return $this->belongsTo(InstitutionDepartment::class, 'institution_department_id');
    }

    public function departmentLevel(): BelongsTo
    {
        return $this->belongsTo(DepartmentLevel::class, 'department_level_id');
    }

    public function departmentCourse(): BelongsTo
    {
        return $this->belongsTo(DepartmentCourse::class, 'department_course_id');
    }

    public function academicYearOption(): BelongsTo
    {
        return $this->belongsTo(AcademicYearOption::class, 'academic_year_option_id');
    }

    public function academicCalendar(): BelongsTo
    {
        return $this->belongsTo(AcademicCalendar::class, 'academic_calendar_id');
    }

    public function modeOfStudy(): BelongsTo
    {
        return $this->belongsTo(ModeOfStudy::class, 'mode_of_study_id');
    }

    public function studentEnrolmentStatus(): BelongsTo
    {
        return $this->belongsTo(StudentEnrolmentStatus::class, 'student_enrolment_status_id');
    }

    public function academicCalendarStudentEnrolment(): HasOne
    {
        return $this->hasOne(
            AcademicCalendarStudentEnrolment::class,
            'student_enrolment_id'
        );
    }

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('StudentEnrolment')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }
}

<?php


/**
 *
 * @mixin Builder
 * @method static filter(DepartmentMetaDataFilter $filters)
 */
class DepartmentLevel extends Model
{
    use HasFactory, SoftDeletes, Filterable, BelongsToTenant, Paginatable, LogsActivity;

    protected $fillable = ['tenant_id', 'institution_department_id', 'level_id', 'description', 'show_on_current_application_period'];

    public function level(): BelongsTo
    {
        return $this->belongsTo(Level::class, 'level_id');
    }

    public function institutionDepartment(): BelongsTo
    {
        return $this->belongsTo(InstitutionDepartment::class, 'institution_department_id');
    }

    public function requirement(): HasOne
    {
        return $this->hasOne(DepartmentLevelRequirement::class, 'department_level_id');
    }

    public function courses(): HasMany
    {
        return $this->hasMany(DepartmentLevelCourse::class, 'department_level_id');
    }

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('DepartmentLevel')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }
}

<?php

/**
 * @mixin Builder
 *
 * @method static filter(SharedNameFilter $filters)
 *
 * @property HigherOrderCollectionProxy|mixed|null $has_application_fee_payment
 */
class Level extends Model
{
    use AssignsPosition, Filterable, HasFactory, LogsActivity, Paginatable, SoftDeletes;

    protected $fillable = ['name', 'position', 'description',
        'allowed_applications_per_level', 'show_on_current_application_period', 'has_application_fee_payment', 'calendar_type'];

    protected function casts(): array
    {
        return [
            'calendar_type' => AcademicCalendarTypeEnum::class,
        ];
    }

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('Level')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }
}
<?php

/**
 *
 * @mixin Builder
 * @method static filter(DepartmentMetaDataFilter $filters)
 */
class DepartmentCourse extends Model
{
    use HasFactory, SoftDeletes, Filterable, BelongsToTenant, Paginatable, LogsActivity;

    protected $fillable = [
        'tenant_id', 'institution_department_id', 'course_id',
        'description', 'show_on_current_application_period'
    ];


    public function course(): BelongsTo
    {
        return $this->belongsTo(Course::class);
    }

    public function institutionDepartment(): BelongsTo
    {
        return $this->belongsTo(InstitutionDepartment::class, 'institution_department_id');
    }

    public function departmentCourseLevels(): HasMany
    {
        return $this->hasMany(DepartmentLevelCourse::class, 'department_course_id')->orderBy('department_level_id');
    }


    public function courseLevelModes(): HasMany
    {
        return $this->hasMany(CourseLevelMode::class, 'department_course_id');
    }

    public function requirement(): HasOne
    {
        return $this->hasOne(CourseRequirement::class, 'department_course_id');
    }

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('DepartmentCourse')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }
}

<?php

/**
 * @mixin Builder
 *
 * @method static filter(SharedNameFilter $filters)
 */
#[ObservedBy([NameSlugObserver::class])]
class AcademicYearOption extends Model
{
    use Filterable, LogsActivity, Paginatable, SoftDeletes;

    protected $fillable = ['name', 'description', 'slug'];

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('AcademicYearOption')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }
}

<?php

/**
 * @mixin Builder
 */
class AcademicCalendar extends Model
{
    use LogsActivity, Paginatable, SoftDeletes;

    protected $fillable = ['calendar_year', 'type', 'opening_date', 'closing_date'];

    /**
     * @return array<string, string>
     */
    protected function casts(): array
    {
        return [
            'type' => AcademicCalendarTypeEnum::class,
        ];
    }

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('AcademicCalendar')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }

    /**
     * Calendars that have started (opening date before today), for a given stored year label.
     */
    public static function queryStartedForCalendarYear(string $calendarYear): Builder
    {
        return static::query()
            ->where('calendar_year', $calendarYear)
            ->whereDate('opening_date', '<', today());
    }

    /**
     * Canonical calendar for Inertia/API links: latest started period in that year.
     */
    public static function resolveCanonicalIdForCalendarYear(string $calendarYear): ?int
    {
        return static::queryStartedForCalendarYear($calendarYear)
            ->orderByDesc('opening_date')
            ->value('id');
    }

    /**
     * @return list<int>
     */
    public static function idsForStartedCalendarYear(string $calendarYear): array
    {
        return static::queryStartedForCalendarYear($calendarYear)
            ->orderByDesc('opening_date')
            ->pluck('id')
            ->map(fn (mixed $id): int => (int) $id)
            ->all();
    }
}
/**
 * @mixin Builder
 *
 * @method static filter(SharedNameFilter $filters)
 */
#[ObservedBy([NameSlugObserver::class])]
class StudentEnrolmentStatus extends Model
{
    use Filterable, HasFactory, LogsActivity, Paginatable, SoftDeletes;

    protected $fillable = ['name', 'description', 'slug', 'color'];

    public function getActivitylogOptions(): LogOptions
    {
        return LogOptions::defaults()
            ->logFillable()
            ->useLogName('StudentEnrolmentStatus')
            ->logOnlyDirty()
            ->dontSubmitEmptyLogs();
    }
} Give the classes above, AcademicCalendar is year of study eg 2026, AcademicYearOption is eg Semester 1, Semester 2. 
CourseSyllabus are courses and CourseSyllabusModle are modules
Run a query that fetchs data and give this structure:
const programme = [
  {
    id: '1',
    level: 'NC',
    course: 'Information Technology',
    code: '25/9088',
    year: 'Year 1 — 2022',
    status: 'Active',
    semesters: [
      {
        id: '1', label: 'Semester 1', year: '2022', status: 'Active',
        module: [
          { code: 'ICT101', name: 'Introduction to Computing',            credits: 4, grade: 'B+', score: 72, lecturer: 'Mr. Moyo',   type: 'Core',    assessment: '2 CAs + Exam' },
          { code: 'ICT102', name: 'Computer Hardware & Maintenance',      credits: 4, grade: 'A',  score: 81, lecturer: 'Ms. Banda',  type: 'Core',    assessment: 'Lab + Exam'   },
          { code: 'MTH101', name: 'Mathematics for Computing I',          credits: 3, grade: 'B',  score: 65, lecturer: 'Mr. Dube',   type: 'Core',    assessment: '2 CAs + Exam' },
          { code: 'COM101', name: 'Communication Skills',                 credits: 3, grade: 'A-', score: 78, lecturer: 'Mrs. Ncube', type: 'General', assessment: 'Portfolio'    },
          { code: 'ICT103', name: 'Introduction to Programming (Python)', credits: 4, grade: 'A',  score: 84, lecturer: 'Mr. Zvobgo', type: 'Core',    assessment: 'Project + Exam'},
        ],
      }
    ],
  },
]